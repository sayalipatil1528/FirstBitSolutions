create function power(a int,b int)returns int
deterministic
begin
	declare i int default 1;
	declare result int default 1;
	while(i<=b) do
		set result=result*a;
		set i=i+1;
	end while;
	return result;
end