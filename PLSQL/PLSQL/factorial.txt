create function factorial(n int)returns int
deterministic
begin
	declare i int default 1;
	declare fact int default 1;
	repeat
	set fact=fact*i;
	set i=i+1;
	until(i>n)
	end repeat;
	return fact;
end