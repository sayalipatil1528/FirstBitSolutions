create function getSalary(id int)returns int
deterministic
begin
	declare sal int;
	select salary into sal from employee where eid=id;
	return sal;
end #


create function getAnnualSalary(id int) returns int
deterministic
begin
	declare sal int;
	select salary into sal from employee where eid=id;
	return sal*12;
end#